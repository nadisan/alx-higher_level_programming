#include "lists.h"

/**
 * is_palindrome - adds a new node at the beginning of a list_t
 * @head: list_t
 *
 * Return: int
 */



int is_palindrome(listint_t **head)
{
	listint_t *fw = *head;

	if (fw == NULL || fw->next == NULL)
		return (1);
	return (1);
}
